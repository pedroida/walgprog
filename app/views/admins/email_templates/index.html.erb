<% provide(:head_title, t('email_template.index')) %>
<% provide(:page_title, t('email_template.index')) %>

<div class="row">
  <div class="col-lg-12">
    <div class="row">
      <div class="col-sm-12 col-lg-8 mb-2">
        <div class="input-group mb-3">
          <div class="input-group">
            <input type="text" class="form-control" placeholder="Procurar...">
            <span class="input-group-append">
              <button class="btn btn-outline-info" type="button"><i class="fe fe-search"></i></button>
            </span>
          </div>
        </div>
      </div>
    </div>

    <div class="table-responsive">
      <table class="table card-table table-striped table-vcenter">
        <thead>
          <tr>
            <th><%= t('activerecord.attributes.email_template.name') %></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <%= render @templates %>
        </tbody>
      </table>
    </div>
  </div>
</div>
